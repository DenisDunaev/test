---
    name: Install nginx
    hosts: all
    become: true
    tasks:
        name: install nginx
        apt:
            name: nginx
            state: present

        name: Enable nginx
        systemd:
            name: nginx
            state: restarted
            enable: yes